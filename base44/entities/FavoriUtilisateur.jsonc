{
  "name": "FavoriUtilisateur",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string"
    },
    "offre_id": {
      "type": "string"
    }
  },
  "required": [
    "user_id",
    "offre_id"
  ],
  "rls": {
    "create": {
      "data.user_id": "{{user.id}}"
    },
    "read": {
      "data.user_id": "{{user.id}}"
    },
    "update": {
      "data.user_id": "{{user.id}}"
    },
    "delete": {
      "data.user_id": "{{user.id}}"
    }
  }
}